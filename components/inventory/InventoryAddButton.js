/**
 * @fileoverview A component which persists over top every element on the Inventory page.
 * The user can click on this button to manually add an inventory item to the list.
 * @author Joshua Couto
 * @version 1.0.0
 */

export default function InventoryAddButton() {
  return (

  );
}